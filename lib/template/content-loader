content_loader(){
    (
	PARAMS=$1
	cat - | while read LINE
	do
	    case ${LINE} in
		*\<==\ content\ [!\ ]*\ ==\>* )
		    echo "${LINE}" |sed s/\<==\ content\ \\\(\[\[\:alnum\:\]-\]*\\\)\ ==\>/\\\1/g ;;
		* )
		    echo "${LINE}" ;;
	    esac
	done		    
    )
}
