include_loader(){
    (
	cat - | while read LINE
	do
	    case ${LINE} in
		\<==\ include\ [!\ ]*\ ==\> )
		    FILENAME=$(echo "${LINE}" | sed s/\<==\ include\ \\\(\[\[\:alnum\:\]\]*\.\[\[\:alnum\:\]\]*\\\)\ ==\>/\\1/g)
		    cat ${COMPONENT_DIR}/${FILENAME} | include_loader ;;
		* )
		    echo "${LINE}" ;;
	    esac
	done		    
    )
}
